<%= form_for(@game_day) do |f| %>
  <% if @game_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game_day.errors.count, "error") %> prohibited this game_day from being saved:</h2>

      <ul>
      <% @game_day.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :weekNumber %><br />
    <%= f.text_field :weekNumber %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
