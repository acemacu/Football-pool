<%= form_for ([@season, @week, @game]) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :home_team_id %><br />
    <%= f.text_field :home_team_id %>
  </div>
  <div class="field">
    <%= f.label :away_team_id %><br />
    <%= f.text_field :away_team_id %>
  </div>
  <div class="field">
    <%= f.label :score_home %><br />
    <%= f.text_field :score_home %>
  </div>
  <div class="field">
    <%= f.label :score_away %><br />
    <%= f.text_field :score_away %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
